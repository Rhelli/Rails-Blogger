<div>
  <p>Comment by: <%= comment.author %></p>
  <p class = 'comment'><%= comment.body %></p>
  <p style='font: 12px/18px italic; text-decoration: underline;color: black;'>Posted <%= distance_of_time_in_words(comment.article.created_at, comment.created_at)%> later</p>
</div>