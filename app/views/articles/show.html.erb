<h1><%= @article.title %></h1>
<p><%= @article.body %></p>

<%= link_to "Delete Article", article_path(@article), method: :delete, data: { confirm: 'Are you sure?' } %><br>
<%= link_to "<<  Back to Latest Articles", articles_path %>